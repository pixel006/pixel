<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Моя команда</title>
    <link rel="stylesheet" href="/style.css">

    <!-- Мета-тег для мобильных устройств -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding-bottom: 80px; /* чтобы футер не перекрывал контент */
        }
        .container {
            max-width: 700px;
            width: 90%; /* адаптивно для мобильных */
            margin: 40px auto;
            padding: 20px;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        h1, h2 {
            margin: 10px 0;
            text-align: center;
            color: #333;
        }
        .ref-link {
            background: #e8f0ff;
            padding: 12px;
            border-radius: 8px;
            margin: 15px 0;
            text-align: center;
        }
        .ref-link button {
            margin-top: 10px;
            background: #5c6bc0;
            color: white;
            padding: 8px 12px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }
        .ref-link button:hover {
            background: #3f4fa3;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            word-break: break-word; /* чтобы текст не вылезал за пределы */
        }
        th, td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
            text-align: left;
        }
        th {
            background-color: #5c6bc0;
            color: white;
        }
        tr:hover {
            background-color: #f1f1f1;
        }
        nav.fixed-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: #fff;
            padding: 12px 0;
            text-align: center;
            box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
        }
        nav.fixed-footer a {
            display: inline-block;
            width: 90%;
            max-width: 250px;
            padding: 12px 0;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }
        nav.fixed-footer a:hover {
            background-color: #3f4fa3;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Моя команда</h1>

    <!-- Реферальный код и ссылка -->
    <div class="ref-link">
        <p><strong>Ваш реферальный код:</strong> <%= currentUser.referralCode %></p>
        <p><strong>Ваша ссылка для приглашений:</strong><br>
        <code><%= request.protocol + '://' + request.get('host') + '/register?ref=' + currentUser.referralCode %></code></p>
        <button onclick="copyLink()">Скопировать ссылку</button>
    </div>

    <h2>Приглашённые пользователи</h2>

    <% if (team.length === 0) { %>
        <p>Пока никто не зарегистрировался по вашей ссылке</p>
    <% } else { %>
        <table>
            <thead>
                <tr>
                    <th>Имя</th>
                    <th>Пополнено ($)</th>
                </tr>
            </thead>
            <tbody>
                <% team.forEach(member => { %>
                    <tr>
                        <td><%= member.name %></td>
                        <td><%= member.balance.toFixed(2) %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</div>

<!-- Фиксированная кнопка внизу -->
<nav class="fixed-footer">
    <a href="/">Главная</a>
</nav><script>
function copyLink() {
    const link = "<%= request.protocol + '://' + request.get('host') + '/register?ref=' + currentUser.referralCode %>";
    navigator.clipboard.writeText(link);
    alert('Ссылка скопирована!');
}
</script>

</body>
</html>