<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Пополнение</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Пополнить баланс пользователя <%= user.name %></h1>

        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <form action="/admin/deposit/<%= user._id %>" method="POST">
            <input type="number" name="amount" placeholder="Сумма" step="0.01" required>
            <button type="submit">Пополнить</button>
        </form>

        <p><a href="/admin">Назад в админку</a></p>
    </div>
</body>
</html>