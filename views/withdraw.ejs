<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<%
  // Безопасные проверки, чтобы не было ReferenceError
  const txSafe = typeof tx !== 'undefined' && tx ? tx : null;
  const userSafe = typeof user !== 'undefined' && user ? user : null;
  const errorSafe = typeof error !== 'undefined' && error ? error : null;
  const currentSafe = typeof currentUser !== 'undefined' && currentUser ? currentUser : null;
%>

<title><%= txSafe ? 'Вывод средств успешен' : (userSafe ? 'Пополнение пользователя' : 'Вывод средств') %></title>
<link rel="stylesheet" href="/style.css">

<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f0f4f8;
        margin: 0;
        padding-bottom: 60px;
    }
    .container {
        max-width: 480px;
        margin: 40px auto;
        padding: 20px;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    h1 {
        text-align: center;
        color: #333;
        margin-bottom: 20px;
        font-size: 22px;
    }
    input, button {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        border-radius: 8px;
        border: 1px solid #ccc;
        font-size: 16px;
        box-sizing: border-box;
    }
    button {
        background-color: #5c6bc0;
        color: white;
        border: none;
        cursor: pointer;
    }
    button:hover {
        background-color: #3f4fa3;
    }
    .error {
        color: red;
        text-align: center;
        margin-bottom: 15px;
    }
    p {
        text-align: center;
    }
</style>
</head>
<body>
<div class="container">

<% if (txSafe) { %>
    <!-- Страница успешного вывода -->
    <h1>Запрос на вывод принят</h1>
    <p>Сумма: $<%= txSafe.amount.toFixed(2) %></p>
    <p>Комиссия 2%: $<%= txSafe.fee.toFixed(2) %></p>
    <p>Адрес для перевода: <%= txSafe.destination %></p>
    <p>Статус: <%= txSafe.status %></p>
    <p><a href="/">Вернуться на главную</a></p>

<% } else if (userSafe) { %>
    <!-- Форма пополнения пользователя (для админа) -->
    <h1>Пополнить баланс пользователя <%= userSafe.name %></h1>
    <% if (errorSafe) { %>
        <p class="error"><%= errorSafe %></p>
    <% } %>
    <form action="/admin/deposit/<%= userSafe._id %>" method="POST">
        <input type="number" name="amount" placeholder="Сумма" step="0.01" required>
        <button type="submit">Пополнить</button>
    </form>
    <p><a href="/admin">Назад в админку</a></p>

<% } else { %>
    <!-- Форма вывода для обычного пользователя -->
    <h1>Проверь адрес кошелька перед выводом:
Сеть должна совпадать (ERC20, TRC20, BEP20 и т.д.).</h1>
    <% if (errorSafe) { %>
        <p class="error"><%= errorSafe %></p>
    <% } %>
    <p>Баланс: $<%= currentSafe && currentSafe.balance ? currentSafe.balance.toFixed(2) : '0.00' %></p>
    <form action="/withdraw" method="POST">
        <input type="number" name="amount" placeholder="Сумма" step="0.01" min="0" required>
        <input type="text" name="cryptoAddress" placeholder="Адрес криптокошелька" required>
        <button type="submit">Подтвердить</button>
    </form>
    <p><a href="/">Назад</a></p>
<% } %>

</div>
</body>
</html>