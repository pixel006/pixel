<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Депозит</title>
<link rel="stylesheet" href="/style.css">
<style>
    body { font-family: Arial; background-color: #f0f4f8; margin: 0; padding-bottom: 60px; }
    .container { max-width: 480px; margin: 40px auto; padding: 20px; background: #fff; border-radius: 12px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); text-align: center; }
    h1 { color: #333; margin-bottom: 20px; font-size: 22px; }

    nav.fixed-footer {
        position: fixed;
        bottom:0;
        left:0;
        width:100%;
        background:#fff;
        padding:12px 0;
        text-align:center;
        box-shadow:0 -2px 5px rgba(0,0,0,0.1);
    }

    nav.fixed-footer a {
        display:inline-block;
        padding:12px 12px;
        margin:0 3px;
        background:#5c6bc0;
        color:#fff;
        text-decoration:none;
        border-radius:6px;
        font-weight:bold;
        font-size:14px;
        transition: background 0.2s;
    }

    nav.fixed-footer a:hover { background:#3f4fa3; }
</style>
</head>
<body>
<div class="container">
    <h1>Депозит</h1>
    <% if (currentUser) { %>
        <p>Баланс: $<%= currentUser.balance.toFixed(2) %></p>
        <p>Нажмите кнопку START внизу для запуска депозита.</p>
    <% } %>
</div>

<!-- Кнопка запуска депозита через GET-запрос -->
<nav class="fixed-footer">
    <a href="/">Главная</a>
    <a href="/group">Моя группа</a>
    <a href="/start-deposit">START</a>
    <a href="/history">История</a>
</nav>
</body>
</html>