<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Пополнение пользователя</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h2>Пополнение пользователя <%= user.name %></h2>

    <% if (error) { %>
      <p style="color:red;"><%= error %></p>
    <% } %>

    <form action="/admin/deposit/<%= user._id %>" method="POST">
      <input type="number" name="amount" placeholder="Сумма в $" step="0.01" required>
      <button type="submit">Пополнить</button>
    </form>

    <a href="/admin">Назад к пользователям</a>
  </div>
</body>
</html>