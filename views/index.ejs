<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Личный кабинет</title>
    <link rel="stylesheet" href="/style.css">
    
    <!-- Мета-тег для мобильных -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <style>
        body {
            margin: 0;
            padding-bottom: 80px; /* чтобы футер не перекрывал контент */
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            color: #333;
        }

        /* --- Шапка --- */
        header {
            background: #f0f4f8; /* сливается с фоном */
            color: #333;
            padding: 15px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            box-shadow: none;
            border-bottom: none;
        }

        a.logout-btn {
            background: #f0f4f8;
            border: none;
            color: #333;
            text-decoration: none;
            padding: 8px 14px;
            border-radius: 6px;
            font-weight: bold;
            transition: background 0.3s, color 0.3s;
        }
        a.logout-btn:hover {
            background: #e0e0e0;
        }

        /* --- Контейнер --- */
        .container {
            max-width: 600px;
            width: 90%; /* адаптивно под экран */
            margin: 40px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            text-align: center;
        }

        .balance {
            font-size: 1.3em;
            margin-bottom: 15px;
        }

        /* --- Кнопки --- */
        .buttons {
            margin-top: 20px;
        }
        .btn {
            display: inline-block;
            width: 80%;       /* адаптивная ширина */
            max-width: 250px; /* не растягивается сильно */
            padding: 12px 0;
            margin: 10px auto;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }
        .btn:hover {
            background-color: #3f4fa3;
        }

        /* --- Футер --- */
        nav.fixed-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: #fff;
            padding: 12px 0;
            text-align: center;
            box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
        }
        nav.fixed-footer a {
            display: inline-block;
            width: 90%;
            max-width: 250px;
            padding: 12px 0;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin: auto;
            transition: background 0.3s;
        }
        nav.fixed-footer a:hover {
            background-color: #3f4fa3;
        }
    </style>
</head>
<body>

    <!-- Шапка с кнопкой выхода -->
    <header>
        <a href="/logout" class="logout-btn">Выйти</a>
    </header>

    <!-- Основной контент -->
    <div class="container">
        <p class="balance"><strong>Баланс:</strong> $<%= currentUser.balance.toFixed(2) %></p>

        <div class="buttons">
            <a href="/deposit" class="btn">Пополнить</a>
            <a href="/withdraw" class="btn">Вывести</a>
        </div>
    </div>

    <!-- Нижняя навигация -->
    <nav class="fixed-footer">
        <a href="/group">Моя группа</a>
    </nav>

</body>
</html>