<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Личный кабинет</title>
    <link rel="stylesheet" href="/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <style>
        body {
            margin: 0;
            padding-bottom: 100px; /* чтобы футер не перекрывал контент */
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            color: #333;
        }

        /* --- Шапка --- */
        header {
            background: #f0f4f8;
            color: #333;
            padding: 15px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        a.logout-btn {
            background: #f0f4f8;
            border: none;
            color: #333;
            text-decoration: none;
            padding: 8px 14px;
            border-radius: 6px;
            font-weight: bold;
            transition: background 0.3s, color 0.3s;
        }
        a.logout-btn:hover {
            background: #e0e0e0;
        }

        /* --- Контейнер --- */
        .container {
            max-width: 600px;
            width: 90%;
            margin: 40px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            text-align: center;
        }

        .balance {
            font-size: 1.3em;
            margin-bottom: 15px;
        }

        /* --- Информационный блок --- */
        .deposit-info {
            font-size: 0.85em;
            color: #555;
            margin: 20px 0;
            text-align: left;
        }
        .deposit-info h3 {
            font-size: 1em;
            margin-bottom: 5px;
            color: #333;
        }
        .deposit-info p {
            margin-bottom: 12px;
            line-height: 1.4;
        }

        /* --- Футер с кнопками --- */
        nav.fixed-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: #fff;
            padding: 12px 0;
            text-align: center;
            box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
        }
        nav.fixed-footer a {
            display: inline-block;
            width: 30%;
            max-width: 150px;
            padding: 12px 0;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
            transition: background 0.3s;
        }
        nav.fixed-footer a:hover {
            background-color: #3f4fa3;
        }
    </style>
</head>
<body>

    <!-- Шапка с кнопкой выхода -->
    <header>
        <a href="/logout" class="logout-btn">Выйти</a>
    </header>

    <!-- Основной контент -->
    <div class="container">
        <% if (currentUser) { %>
            <p class="balance"><strong>Баланс:</strong> $<%= currentUser.balance.toFixed(2) %></p>
            <p>Нажмите кнопку START внизу для запуска депозита.</p>
        <% } %>

        <!-- Информационный блок о депозите -->
        <div class="deposit-info">
            <h3>Запуск депозита</h3>
            <p>Введённые средства блокируются в системе и отображаются как «в работе» до окончания периода регулирования.</p>

            <h3>Начисление доходности</h3>
            <p>На депозит начисляется 5% ежедневно. Начисленные проценты становятся доступными для вывода по воскресеньям с 08:00 до 20:00. Первоначальная сумма депозита остаётся заблокированной до истечения минимального срока регулирования — один месяц.</p><h3>Реферальная программа</h3>
            <p>Если вы приглашаете новых участников, и они открывают депозит, вы получаете 10% от суммы депозита приглашённого пользователя. Вознаграждение начисляется на ваш баланс автоматически.</p>
        </div>
    </div>

    <!-- Нижняя навигация с кнопкой START -->
    <nav class="fixed-footer">
        <a href="/group">Моя группа</a>
        <a id="depositBtn" class="btn">START</a>
        <a href="/history">История</a>
    </nav>

    <script>
    document.addEventListener('DOMContentLoaded', () => {
        const btn = document.getElementById('depositBtn');
        if (btn) {
            btn.addEventListener('click', async () => {
                try {
                    const res = await fetch('/start-deposit', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ amount: 100 }) // фиксированная сумма
                    });
                    if (res.redirected) window.location.href = res.url;
                } catch (err) {
                    alert('Ошибка сервера');
                    console.error(err);
                }
            });
        }
    });
    </script>

</body>
</html>