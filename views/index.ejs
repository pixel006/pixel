<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Личный кабинет</title>
    <link rel="stylesheet" href="/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <style>
        body {
            margin: 0;
            padding-bottom: 100px; 
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            color: #333;
        }

        header {
            background: #f0f4f8;
            color: #333;
            padding: 15px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        a.logout-btn {
            background: #f0f4f8;
            border: none;
            color: #333;
            text-decoration: none;
            padding: 8px 14px;
            border-radius: 6px;
            font-weight: bold;
            transition: background 0.3s, color 0.3s;
        }
        a.logout-btn:hover {
            background: #e0e0e0;
        }

        .container {
            max-width: 600px;
            width: 90%;
            margin: 40px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            text-align: center;
        }

        .balance {
            font-size: 1.3em;
            margin-bottom: 15px;
        }

        .buttons, .deposit-form {
            margin-top: 20px;
        }

        .btn, .deposit-form button {
            display: inline-block;
            width: 80%;
            max-width: 250px;
            padding: 12px 0;
            margin: 10px auto;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }

        .btn:hover, .deposit-form button:hover {
            background-color: #3f4fa3;
        }

        .deposit-form input {
            width: 80%;
            max-width: 250px;
            padding: 10px;
            margin: 10px auto;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
            display: block;
        }

        nav.fixed-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: #fff;
            padding: 12px 0;
            text-align: center;
            box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
        }

        nav.fixed-footer a {
            display: inline-block;
            width: 45%;
            max-width: 200px;
            padding: 12px 0;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
            text-align: center;
        }
        nav.fixed-footer a:hover { background-color: #3f4fa3; }

    </style>
</head>
<body>

<header>
    <a href="/logout" class="logout-btn">Выйти</a>
</header>

<div class="container">
    <p class="balance"><strong>Баланс:</strong> $<%= currentUser.balance.toFixed(2) %></p>

    <div class="buttons">
        <a href="/deposit" class="btn">Пополнить</a>
        <a href="/withdraw" class="btn">Вывести</a>
    </div>

    <div class="deposit-form">
        <h2>Запустить депозит</h2>
        <form action="/start-deposit" method="POST">
            <input type="number" name="amount" placeholder="Сумма в $" step="0.01" required>
            <button type="submit">Запустить депозит</button>
        </form>
    </div>
</div>

<nav class="fixed-footer">
    <a href="/group">Моя группа</a>
    <a href="/history">История</a>
</nav>

</body>
</html>