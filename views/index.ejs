<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Главная</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            margin: 0;
            padding-bottom: 60px; /* Чтобы контент не прятался за футером */
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
        }
        .container {
            max-width: 600px;
            margin: 40px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            text-align: center;
        }
        h2 {
            margin: 20px 0;
            font-weight: normal;
        }
        .buttons {
            margin-top: 20px;
        }
        .btn {
            display: inline-block;
            padding: 12px 25px;
            margin: 10px;
            background-color: #5c6bc0; /* синий цвет */
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }
        .btn:hover {
            background-color: #5c6bc0;
        }
        nav.fixed-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background: #fff;
            padding: 12px 0;
            text-align: center;
            box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
        }
        nav.fixed-footer a {
            display: inline-block;
            padding: 12px 25px;
            background-color: #5c6bc0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }
        nav.fixed-footer a:hover {
            background-color: #5c6bc0;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>Баланс: $<%= currentUser.balance.toFixed(2) %></h2>

    <div class="buttons">
        <a href="/deposit" class="btn">Пополнить</a>
        <a href="/withdraw" class="btn">Вывести</a>
    </div>
</div>

<nav class="fixed-footer">
    <a href="/group">Группа</a>
</nav>

</body>
</html>